pop<- read.csv(file=url("http://www1.appstate.edu/~thomleyje/R-datafiles/Popular.csv"))
table(pop$Urban.Rural)
table(pop$Grade)
prop.table(table(pop$Gender))
prop.table(table(pop$Gender,pop$Urban.Rural))
table(pop$Goals)
table(pop$Gender,pop$Goals)
prop.table(table(pop$Gender,pop$Goals),2))
p<-subset(pop,Goals=="Popularity")
table(pop$Grades)
table(pop$Sports)
table(pop$Looks)
table(pop$Money)
table(pop$Gender,pop$Grades) 
table(pop$Gender,pop$Sports)
table(pop$Gender,pop$Looks)
table(pop$Gender,pop$Money)
table(pop$Urban.Rural,pop$Grades) 
table(pop$Urban.Rural,pop$Sports)
table(pop$Urban.Rural,pop$Looks)
table(pop$Urban.Rural,pop$Money)